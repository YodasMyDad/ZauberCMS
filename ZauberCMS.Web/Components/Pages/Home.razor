@page "/"
@using System.Net.Mime
@using ZauberCMS.Web.TestData

<PageTitle>Home</PageTitle>


<ContentType Properties="@_contentProperties"/>

@code {

    private readonly List<ContentPropertyData> _contentProperties =
    [
        new ContentPropertyData
        {
            Alias = "Title",
            ComponentType = typeof(TextContentProperty),
            Value = "Initial Title"
        },

        new ContentPropertyData
        {
            Alias = "Description",
            ComponentType = typeof(TextAreaContentProperty),
            Value = "Initial Description"
        }
    ];

}