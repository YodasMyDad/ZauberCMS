---
alwaysApply: true
---

## MediatR to DI Service Migration Task List

You are methodically and without breaking any functionality going to remove mediator from this project by following these tasks.

This task list must be followed exactly, each phase must be complete before moving to the next phase. Each task within the phase must be completed in a step by step format. Once the task is complete it will be marked as complete like so [X] - 

So tasks that need completing are [] and completed tasks will be [X]

### Phase 1: Service Interface Creation
[X] - Create `IContentService` interface in `ZauberCMS.Core/Content/Interfaces/`
[X] - Create `IMembershipService` interface in `ZauberCMS.Core/Membership/Interfaces/`
[X] - Create `IMediaService` interface in `ZauberCMS.Core/Media/Interfaces/`
[X] - Create `ILanguageService` interface in `ZauberCMS.Core/Languages/Interfaces/`
[X] - Create `ITagService` interface in `ZauberCMS.Core/Tags/Interfaces/`
[X] - Create `IAuditService` interface in `ZauberCMS.Core/Audit/Interfaces/`
[X] - Create `ISeoService` interface in `ZauberCMS.Core/Seo/Interfaces/`
[X] - Create `IDataService` interface in `ZauberCMS.Core/Data/Interfaces/`
[X] - Create `IEmailService` interface in `ZauberCMS.Core/Email/Interfaces/`

### Phase 2: Parameter Classes Creation

#### Content Service Parameters
[X] - Create `GetContentParameters` class in `ZauberCMS.Core/Content/Parameters/`
[X] - Create `SaveContentParameters` class in `ZauberCMS.Core/Content/Parameters/`
[X] - Create `QueryContentParameters` class in `ZauberCMS.Core/Content/Parameters/`
[X] - Create `DeleteContentParameters` class in `ZauberCMS.Core/Content/Parameters/`
[X] - Create `CopyContentParameters` class in `ZauberCMS.Core/Content/Parameters/`
[X] - Create `GetContentFromRequestParameters` class in `ZauberCMS.Core/Content/Parameters/`
[X] - Create `GetContentTypeParameters` class in `ZauberCMS.Core/Content/Parameters/`
[X] - Create `SaveContentTypeParameters` class in `ZauberCMS.Core/Content/Parameters/`
[X] - Create `QueryContentTypesParameters` class in `ZauberCMS.Core/Content/Parameters/`
[X] - Create `DeleteContentTypeParameters` class in `ZauberCMS.Core/Content/Parameters/`
[X] - Create `GetDomainParameters` class in `ZauberCMS.Core/Content/Parameters/`
[X] - Create `SaveDomainParameters` class in `ZauberCMS.Core/Content/Parameters/`
[X] - Create `QueryDomainParameters` class in `ZauberCMS.Core/Content/Parameters/`
[X] - Create `DeleteDomainParameters` class in `ZauberCMS.Core/Content/Parameters/`
[X] - Create `AnyContentParameters` class in `ZauberCMS.Core/Content/Parameters/`
[X] - Create `HasChildContentParameters` class in `ZauberCMS.Core/Content/Parameters/`
[X] - Create `HasChildContentTypeParameters` class in `ZauberCMS.Core/Content/Parameters/`
[X] - Create `GetContentLanguagesParameters` class in `ZauberCMS.Core/Content/Parameters/`
[X] - Create `CachedDomainsParameters` class in `ZauberCMS.Core/Content/Parameters/`
[X] - Create `ClearUnpublishedContentParameters` class in `ZauberCMS.Core/Content/Parameters/`
[X] - Create `DataGridContentParameters` class in `ZauberCMS.Core/Content/Parameters/`

#### Membership Service Parameters
[X] - Create `GetUserParameters` class in `ZauberCMS.Core/Membership/Parameters/`
[X] - Create `SaveUserParameters` class in `ZauberCMS.Core/Membership/Parameters/`
[X] - Create `CreateUpdateUserParameters` class in `ZauberCMS.Core/Membership/Parameters/`
[X] - Create `DeleteUserParameters` class in `ZauberCMS.Core/Membership/Parameters/`
[X] - Create `QueryUsersParameters` class in `ZauberCMS.Core/Membership/Parameters/`
[X] - Create `GetRoleParameters` class in `ZauberCMS.Core/Membership/Parameters/`
[X] - Create `SaveRoleParameters` class in `ZauberCMS.Core/Membership/Parameters/`
[X] - Create `DeleteRoleParameters` class in `ZauberCMS.Core/Membership/Parameters/`
[X] - Create `QueryRolesParameters` class in `ZauberCMS.Core/Membership/Parameters/`
[X] - Create `LoginUserParameters` class in `ZauberCMS.Core/Membership/Parameters/`
[X] - Create `RegisterUserParameters` class in `ZauberCMS.Core/Membership/Parameters/`
[X] - Create `ExternalLoginParameters` class in `ZauberCMS.Core/Membership/Parameters/`
[X] - Create `ConfirmEmailParameters` class in `ZauberCMS.Core/Membership/Parameters/`
[X] - Create `ForgotPasswordParameters` class in `ZauberCMS.Core/Membership/Parameters/`
[X] - Create `ResetPasswordParameters` class in `ZauberCMS.Core/Membership/Parameters/`
[X] - Create `GetCurrentUserParameters` class in `ZauberCMS.Core/Membership/Parameters/`

#### Media Service Parameters
[X] - Create `GetMediaParameters` class in `ZauberCMS.Core/Media/Parameters/`
[X] - Create `SaveMediaParameters` class in `ZauberCMS.Core/Media/Parameters/`
[X] - Create `QueryMediaParameters` class in `ZauberCMS.Core/Media/Parameters/`
[X] - Create `DeleteMediaParameters` class in `ZauberCMS.Core/Media/Parameters/`
[X] - Create `HasChildMediaParameters` class in `ZauberCMS.Core/Media/Parameters/`
[X] - Create `GetRestrictedMediaUrlsParameters` class in `ZauberCMS.Core/Media/Parameters/`

#### Language Service Parameters
[X] - Create `GetLanguageParameters` class in `ZauberCMS.Core/Languages/Parameters/`
[X] - Create `SaveLanguageParameters` class in `ZauberCMS.Core/Languages/Parameters/`
[X] - Create `QueryLanguageParameters` class in `ZauberCMS.Core/Languages/Parameters/`
[X] - Create `DeleteLanguageParameters` class in `ZauberCMS.Core/Languages/Parameters/`
[X] - Create `SaveLanguageDictionaryParameters` class in `ZauberCMS.Core/Languages/Parameters/`
[X] - Create `DeleteLanguageDictionaryParameters` class in `ZauberCMS.Core/Languages/Parameters/`
[X] - Create `GetCachedAllLanguageDictionariesParameters` class in `ZauberCMS.Core/Languages/Parameters/`
[X] - Create `DataGridLanguageDictionaryParameters` class in `ZauberCMS.Core/Languages/Parameters/`

#### Tag Service Parameters
[X] - Create `SaveTagParameters` class in `ZauberCMS.Core/Tags/Parameters/`
[X] - Create `QueryTagParameters` class in `ZauberCMS.Core/Tags/Parameters/`
[X] - Create `DeleteTagParameters` class in `ZauberCMS.Core/Tags/Parameters/`
[X] - Create `SaveTagItemParameters` class in `ZauberCMS.Core/Tags/Parameters/`
[X] - Create `DeleteTagItemParameters` class in `ZauberCMS.Core/Tags/Parameters/`

#### Audit Service Parameters
[X] - Create `SaveAuditParameters` class in `ZauberCMS.Core/Audit/Parameters/`
[X] - Create `QueryAuditsParameters` class in `ZauberCMS.Core/Audit/Parameters/`
[X] - Create `CleanupOldAuditsParameters` class in `ZauberCMS.Core/Audit/Parameters/`

#### SEO Service Parameters
[X] - Create `SaveRedirectParameters` class in `ZauberCMS.Core/Seo/Parameters/`
[X] - Create `QueryRedirectsParameters` class in `ZauberCMS.Core/Seo/Parameters/`
[X] - Create `DeleteRedirectParameters` class in `ZauberCMS.Core/Seo/Parameters/`

#### Data Service Parameters
[X] - Create `GetGlobalDataParameters` class in `ZauberCMS.Core/Data/Parameters/`
[X] - Create `SaveGlobalDataParameters` class in `ZauberCMS.Core/Data/Parameters/`
[X] - Create `MultiQueryParameters` class in `ZauberCMS.Core/Data/Parameters/`
[X] - Create `DataGridParameters<T>` class in `ZauberCMS.Core/Data/Parameters/`

#### Email Service Parameters
[X] - Create `SendEmailConfirmationParameters` class in `ZauberCMS.Core/Email/Parameters/`

### Phase 3: Service Implementation

#### Content Service Implementation - Make sure you take the logic from the handlers and put it in the services. Do not use Mediator directly in these services, they are a direct replacement.
[X] - Create `ContentService` class in `ZauberCMS.Core/Content/Services/`
[X] - Implement `GetContentAsync` method in `ContentService`
[X] - Implement `SaveContentAsync` method in `ContentService`
[X] - Implement `QueryContentAsync` method in `ContentService`
[X] - Implement `DeleteContentAsync` method in `ContentService`
[X] - Implement `CopyContentAsync` method in `ContentService`
[X] - Implement `GetContentFromRequestAsync` method in `ContentService`
[X] - Implement `GetContentTypeAsync` method in `ContentService`
[X] - Implement `SaveContentTypeAsync` method in `ContentService`
[X] - Implement `QueryContentTypesAsync` method in `ContentService`
[X] - Implement `DeleteContentTypeAsync` method in `ContentService`
[X] - Implement `GetDomainAsync` method in `ContentService`
[X] - Implement `SaveDomainAsync` method in `ContentService`
[X] - Implement `QueryDomainAsync` method in `ContentService`
[X] - Implement `DeleteDomainAsync` method in `ContentService`
[X] - Implement `AnyContentAsync` method in `ContentService`
[X] - Implement `HasChildContentAsync` method in `ContentService`
[X] - Implement `HasChildContentTypeAsync` method in `ContentService`
[X] - Implement `GetContentLanguagesAsync` method in `ContentService`
[X] - Implement `GetCachedDomainsAsync` method in `ContentService`
[X] - Implement `ClearUnpublishedContentAsync` method in `ContentService`
[X] - Implement `GetDataGridContentAsync` method in `ContentService`

#### Membership Service Implementation
[X] - Create `MembershipService` class in `ZauberCMS.Core/Membership/Services/`
[X] - Implement `GetUserAsync` method in `MembershipService`
[X] - Implement `SaveUserAsync` method in `MembershipService`
[X] - Implement `CreateUpdateUserAsync` method in `MembershipService`
[X] - Implement `DeleteUserAsync` method in `MembershipService`
[X] - Implement `QueryUsersAsync` method in `MembershipService`
[X] - Implement `GetRoleAsync` method in `MembershipService`
[X] - Implement `SaveRoleAsync` method in `MembershipService`
[X] - Implement `DeleteRoleAsync` method in `MembershipService`
[X] - Implement `QueryRolesAsync` method in `MembershipService`
[X] - Implement `LoginUserAsync` method in `MembershipService`
[X] - Implement `RegisterUserAsync` method in `MembershipService`
[X] - Implement `ExternalLoginAsync` method in `MembershipService`
[X] - Implement `ConfirmEmailAsync` method in `MembershipService`
[X] - Implement `ForgotPasswordAsync` method in `MembershipService`
[X] - Implement `ResetPasswordAsync` method in `MembershipService`
[X] - Implement `GetCurrentUserAsync` method in `MembershipService`

#### Media Service Implementation
[X] - Create `MediaService` class in `ZauberCMS.Core/Media/Services/`
[X] - Implement `GetMediaAsync` method in `MediaService`
[X] - Implement `SaveMediaAsync` method in `MediaService`
[X] - Implement `QueryMediaAsync` method in `MediaService`
[X] - Implement `DeleteMediaAsync` method in `MediaService`
[X] - Implement `HasChildMediaAsync` method in `MediaService`
[X] - Implement `GetRestrictedMediaUrlsAsync` method in `MediaService`

#### Language Service Implementation
[X] - Create `LanguageService` class in `ZauberCMS.Core/Languages/Services/`
[X] - Implement `GetLanguageAsync` method in `LanguageService`
[X] - Implement `SaveLanguageAsync` method in `LanguageService`
[X] - Implement `QueryLanguageAsync` method in `LanguageService`
[X] - Implement `DeleteLanguageAsync` method in `LanguageService`
[X] - Implement `SaveLanguageDictionaryAsync` method in `LanguageService`
[X] - Implement `DeleteLanguageDictionaryAsync` method in `LanguageService`
[X] - Implement `GetCachedAllLanguageDictionariesAsync` method in `LanguageService`
[X] - Implement `GetDataGridLanguageDictionaryAsync` method in `LanguageService`

#### Tag Service Implementation
[X] - Create `TagService` class in `ZauberCMS.Core/Tags/Services/`
[X] - Implement `SaveTagAsync` method in `TagService`
[X] - Implement `QueryTagAsync` method in `TagService`
[X] - Implement `DeleteTagAsync` method in `TagService`
[X] - Implement `SaveTagItemAsync` method in `TagService`
[X] - Implement `DeleteTagItemAsync` method in `TagService`

#### Audit Service Implementation
[X] - Create `AuditService` class in `ZauberCMS.Core/Audit/Services/`
[X] - Implement `SaveAuditAsync` method in `AuditService`
[X] - Implement `QueryAuditsAsync` method in `AuditService`
[X] - Implement `CleanupOldAuditsAsync` method in `AuditService`

#### SEO Service Implementation
[X] - Create `SeoService` class in `ZauberCMS.Core/Seo/Services/`
[X] - Implement `SaveRedirectAsync` method in `SeoService`
[X] - Implement `QueryRedirectsAsync` method in `SeoService`
[X] - Implement `DeleteRedirectAsync` method in `SeoService`

#### Data Service Implementation
[X] - Create `DataService` class in `ZauberCMS.Core/Data/Services/`
[X] - Implement `GetGlobalDataAsync` method in `DataService`
[X] - Implement `SaveGlobalDataAsync` method in `DataService`
[X] - Implement `MultiQueryAsync` method in `DataService`
[X] - Implement `GetDataGridAsync<T>` method in `DataService`

#### Email Service Implementation
[X] - Create `EmailService` class in `ZauberCMS.Core/Email/Services/`
[X] - Implement `SendEmailConfirmationAsync` method in `EmailService`

### Phase 3.1: Data verification - It's imperative that this is a like for like change to services. It should be identical just using services and parameter classes. Take time to think and check everything.
[] - Verify all parameter classes have identical properties to their corresponding command classes (Check inherited classes if there are any) - Do not add any other properties.
[] - Verify all service methods have identical signatures to their corresponding handler methods - Do not add any other code, remove it if you have added something different
[] - Verify all service implementations maintain the same business logic as their handler counterparts - Do not add any other code, remove it if you have added something different

### Phase 4: Service Registration
[] - Register all service interfaces and implementations in `ZauberSetup.cs`
[] - Register `IContentService` with `ContentService` implementation
[] - Register `IMembershipService` with `MembershipService` implementation
[] - Register `IMediaService` with `MediaService` implementation
[] - Register `ILanguageService` with `LanguageService` implementation
[] - Register `ITagService` with `TagService` implementation
[] - Register `IAuditService` with `AuditService` implementation
[] - Register `ISeoService` with `SeoService` implementation
[] - Register `IDataService` with `DataService` implementation
[] - Register `IEmailService` with `EmailService` implementation

### Phase 5: Validation and Testing
[] - Test that all services can be resolved from DI container
[] - Verify that no functionality has been changed or broken during the migration